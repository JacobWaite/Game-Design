class movingState extends state {
    constructor(stateMachine) {
        super(stateMachine);
        
    }
     do(direction) {
        if("left") {
            stateMachine.parent.animation
        }
     }
    
}